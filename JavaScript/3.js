//interactions -- alert, prompt, confirm (functions)

alert("New Page!!");

//prompt(title, [input]);
let date = prompt("today's date", ["dd/mm/yy"]);
let day = prompt("Day is");

//confirm(ques)
let time = confirm("time's up!!");

alert(date + " " + day);